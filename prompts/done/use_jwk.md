migrate the application to use jwk tokens via jose that have an expiry of 15 mins and store a login token in the database instead.

Have the server generate a key automatically if one does not exist.
